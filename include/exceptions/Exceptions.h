// ставим защиту от повторного подключения заголовка
#pragma once
// подтягиваем stdexcept чтобы здесь были нужные типы и функции
#include <stdexcept>
// подтягиваем string чтобы здесь были нужные типы и функции
#include <string>

// заходим в пространство имен чтобы не засорять глобальную область
namespace am {

// этим комментом помогаем быстрее считать логику глазами
// этот class AmException нужен чтобы держать рядом данные и связанную логику
// внутри мы собираем единый контракт чтобы модуль было проще читать и расширять
// тут просто продолжаем собирать итоговый результат
class AmException : public std::runtime_error {
// тут лежит публичный интерфейс который видят снаружи
public:
// эта функция AmException решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// этой строкой фиксируем промежуточный результат
    explicit AmException(const std::string& msg) : std::runtime_error(msg) {}
// закрываем объявление типа и заканчиваем этот блок
};

// этот class ConfigError нужен чтобы держать рядом данные и связанную логику
// внутри мы собираем единый контракт чтобы модуль было проще читать и расширять
// здесь проводим маленький но важный технический шаг
class ConfigError : public AmException {
// тут лежит публичный интерфейс который видят снаружи
public:
// эта функция ConfigError решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// этой строкой фиксируем промежуточный результат
    explicit ConfigError(const std::string& msg) : AmException("ConfigError: " + msg) {}
// закрываем объявление типа и заканчиваем этот блок
};

// этот class CsvError нужен чтобы держать рядом данные и связанную логику
// внутри мы собираем единый контракт чтобы модуль было проще читать и расширять
// тут просто продолжаем собирать итоговый результат
class CsvError : public AmException {
// тут лежит публичный интерфейс который видят снаружи
public:
// эта функция CsvError решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// на этом месте держим логику прямой и читаемой
    explicit CsvError(const std::string& msg) : AmException("CsvError: " + msg) {}
// закрываем объявление типа и заканчиваем этот блок
};

// этот class DataValidationError нужен чтобы держать рядом данные и связанную логику
// внутри мы собираем единый контракт чтобы модуль было проще читать и расширять
// тут просто продолжаем собирать итоговый результат
class DataValidationError : public AmException {
// тут лежит публичный интерфейс который видят снаружи
public:
// эта функция DataValidationError решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// здесь проводим маленький но важный технический шаг
    explicit DataValidationError(const std::string& msg) : AmException("DataValidationError: " + msg) {}
// закрываем объявление типа и заканчиваем этот блок
};

// этот class CalculationError нужен чтобы держать рядом данные и связанную логику
// внутри мы собираем единый контракт чтобы модуль было проще читать и расширять
// здесь аккуратно готовим данные для следующего шага
class CalculationError : public AmException {
// тут лежит публичный интерфейс который видят снаружи
public:
// эта функция CalculationError решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// тут закрываем небольшой кусок общей задачи
    explicit CalculationError(const std::string& msg) : AmException("CalculationError: " + msg) {}
// закрываем объявление типа и заканчиваем этот блок
};

// на этом месте держим логику прямой и читаемой
} // закрываем пространство имен am


